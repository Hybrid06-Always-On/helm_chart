Frontend (WEB) has been deployed successfully 🚀

Deployment Information:
- Name: frontend-deploy
- Replicas: {{ .Values.replicaCount }}
- Service Type: {{ .Values.service.type }}

Access Method:
1. Check Service:
   kubectl get svc {{ .Values.service.name }} -n {{ .Release.Namespace }}

2. If using MetalLB:
   Access the EXTERNAL-IP assigned to the frontend service.

Notes:
- Frontend is a static web application (HTML/CSS/JS).
- Backend API should be accessed via Kubernetes Service DNS.
  Example:
    http://backend-svc:3000

- No ConfigMap or Secret is used for frontend runtime configuration.
